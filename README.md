# 👑 [KING]DevSupport AutoPanel PRO

Install:

Step 2 — Clone 
git clone https://github.com/SLSTunnel/AutoPanel.git
cd AutoPanel

🔹 Step 3 — VERIFY FILES (VERY IMPORTANT)
ls

You should see:

core  services  web  autopanel  install.sh
🔹 Step 4 — Run install properly

chmod +x install.sh
./install.sh

🔹 Step 5 — Fix autopanel command (if still missing)
chmod +x autopanel
mv autopanel /usr/bin/

Then run:

autopanel

Run:
autopanel

Web:
http://IP/web/
